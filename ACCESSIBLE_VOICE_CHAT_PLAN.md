# נגישות — קול וצ'אט לתכנון טיול (תוכנית פיצ'ר)

_עודכן לאחרונה: 2026-06-13 • מסמך תכנון בלבד. לא שונה קוד אפליקציה ולא בוצע commit._
_מסמך נלווה ל-`ACCESSIBLE_TRAVEL_PRODUCT_PLAN.md`, `ACCESSIBLE_TRAVEL_ROADMAP.md` ו-`ACCESSIBLE_TRAVEL_NEXT_SPRINT.md`._
_**אינטגרציית Wheelmap לא מתחילה כאן** — היא ממתינה לספייק נפרד (Next Sprint משימה 6)._

---

## מטרת־על

אנשים עם מוגבלות צריכים להיות מסוגלים לתכנן טיול **בדיבור או בכתיבה**, בעברית, ולשלוח מידע אל המתכנן — קישורים למלונות ואטרקציות, הערות, צרכי נגישות ובהמשך גם תמונות. הקול והצ'אט הם **ערוץ קלט נגיש נוסף** למסלול הקיים, לא מנוע חדש: כל מה שנקלט מתמפה לאותו פרופיל נגישות (`trippilot:a11y-profile`) ולאותו חוזה יצירת מסלול (`generate-itinerary.ts`).

**אינוריאנט מנחה (יורש מהספרינט):** לעולם לא מציגים נגישות לא מאומתת כמאומתת. כל קלט קולי/צ'אט/קישור שמייצר טענת נגישות יקבל מקור + זמן בדיקה + רמת ודאות, ובהיעדרם יוצג **"נגישות לא מאומתת — מומלץ לבדוק מול המקום"**. הצ'אט לא ממציא עובדות נגישות.

---

## 1. קלט קולי לתכנון טיול

- **מה המשתמש עושה:** לוחץ על כפתור מיקרופון גדול ("דברו אליי"), אומר בעברית משפט חופשי — למשל _"אני בכיסא גלגלים, רוצה ארבעה ימים בברצלונה, מלונות עם גישה ללא מדרגות וקצב רגוע"_ — והמערכת ממירה דיבור לטקסט, מחלצת ישויות ומציגה אישור לפני שליחה למתכנן.
- **גישה טכנית (MVP):** שימוש ב-`Web Speech API` (`SpeechRecognition`) של הדפדפן עם `lang="he-IL"`, מאחורי קומפוננטה אחת `voice-input.tsx`. אין תלות בספק חיצוני ל-MVP. שכבת `voice-capture` מחזירה טקסט גולמי בלבד; שכבת `parseTravelUtterance(text)` (pure, נטולת framework, בת-בדיקה) ממפה אותו אל שדות `CustomItineraryInput` קיימים: יעד, מספר ימים, סוג טיול, וצרכי נגישות → `AccessibilityPreferences`.
- **חילוץ ישויות:** התחלה כחילוץ דטרמיניסטי קל (מילון מילות מפתח עברית: "כיסא גלגלים", "ללא מדרגות", "קצב רגוע", "הליכון", מספרים/ימים, שמות ערים). שדרוג עתידי: שליחת התמלול ל-`openai-agent` / מודל Claude לחילוץ מובנה (JSON tool-call) כאשר ספק AI מחובר — אך לעולם לא חוסם: ללא ספק, נופלים חזרה לחילוץ המקומי.
- **אישור לפני שליחה:** המערכת מציגה "הבנתי כך…" עם הערכים שזוהו, ומאפשרת לתקן בטקסט או בקול לפני יצירת המסלול. דיבור לא יוצר מסלול אוטומטית ללא אישור (ראו §8).
- **נפילה אלגנטית:** אם הדפדפן לא תומך ב-`SpeechRecognition` (למשל פיירפוקס/חלק מהמובייל), הכפתור מסתתר ומוצגת חלופת הקלדה זהה־פונקציונלית. אין מסך שבור.

## 2. עוזר צ'אט לשאלות נגישות

- **מה המשתמש עושה:** מקליד או מכתיב שאלת נגישות חופשית — _"האם המלון שבחרתי מתאים לכיסא גלגלים?"_, _"איך מגיעים מהמלון לסגרדה פמיליה בלי מדרגות?"_, _"מה זה נגישות לא מאומתת?"_ — ומקבל תשובה בעברית, מעוגנת בנתוני המסלול והפרופיל הקיימים.
- **גישה טכנית:** הרחבת ה-stub הקיים `src/components/ai-chat-assistant.tsx` (כיום סטטי) לצ'אט אמיתי. ה-prompt מוזרק עם הקשר: הפרופיל הנגיש (§6), המסלול הנוכחי אם קיים, וכלל היושרה. בהיעדר ספק AI — מצב מקומי שעונה משאלות־תבנית (FAQ נגישות) ומפנה לנתונים שכבר במסך, במקום להמציא.
- **גבולות גזרה (guardrails):** העוזר **לא** מאשר נגישות של מקום ספציפי מתוך ידע כללי. כשנשאל "האם X נגיש?" הוא עונה רק לפי שדות `AccessibilityFact` המאומתים שכבר נטענו; אם הם חסרים/`unverified` — הוא אומר זאת מפורשות ומציע "לבדוק מול המקום". זהו אותו כלל יושרה של הכרטיסים, באכיפה שיחתית.
- **תבניות שימוש:** הסבר על מסננים ומונחי נגישות, ניווט באפליקציה ("איפה משנים את פרופיל הנגישות?"), וסיכום צרכי הנגישות שזוהו — לא ייעוץ רפואי ולא הבטחות.

## 3. שליחה/צירוף מידע אל המתכנן

- **מה נשלח:** קישורי מלון, קישורי אטרקציה, הערות חופשיות, צרכי נגישות, ובהמשך תמונות.
- **קישורים (MVP):** המשתמש מדביק קישור; שכבת `parseSharedLink(url)` מזהה סוג (מלון / אטרקno / לא ידוע) ושומרת אותו כ**הקשר משתמש**, לא כעובדת נגישות מאומתת. קישור חיצוני שמתפרסם נחשב פעולה החוצה — לא מבצעים fetch אוטומטי לתוכן צד-שלישי ב-MVP; שומרים URL + תווית בלבד. שליפת מטא-דאטה (כותרת/נגישות) — שלב עתידי, מאחורי הסכמת משתמש ועם תיוג מקור.
- **הערות וצרכי נגישות:** טקסט חופשי שמתווסף ל-`CustomItineraryInput.preferences` כ"הערות מתכנן" ומשפיע על ה-prompt של יצירת המסלול. צרכי נגישות שזוהו מהדיבור/צ'אט מתמזגים לתוך `AccessibilityPreferences` ולפרופיל המקומי (§6).
- **תמונות (שלב מאוחר, מסומן "later"):** העלאת תמונה (למשל צילום כניסת מלון) תיתמך בעתיד עם תיאור־נגישות אנושי/אוטומטי. ב-MVP מוגדר רק חוזה הנתונים (`attachments: Attachment[]` עם `type: 'link' | 'note' | 'photo'`), ללא מימוש העלאה. תמונות לעולם לא הופכות אוטומטית ל"נגישות מאומתת".
- **מודל נתונים מוצע (טיוטה, לא ממומש):**
  ```ts
  type SharedAttachment =
    | { kind: "link"; url: string; label: string; guessedType: "hotel" | "attraction" | "unknown" }
    | { kind: "note"; text: string }
    | { kind: "accessibility-need"; key: string; value: string }
    | { kind: "photo"; ref: string; caption?: string }; // later
  ```

## 4. תמיכה עברית-first

- כל ה-UI, הודעות הקול, תשובות הצ'אט וה-placeholders — בעברית כברירת מחדל, RTL. מפתחות i18n חדשים תחת `voice.*` ו-`chat.*` ב-`messages/he.json` (מקור האמת), ואז `en.json` ושאר השפות.
- זיהוי דיבור עם `lang="he-IL"`; חילוץ הישויות מתחיל ממילון עברית. אנגלית וערבית הן הרחבה (הפרויקט כבר תומך ar/en/es/fr/ru).
- מונחי נגישות מתורגמים דרך `ui-translations.ts` הקיים כדי לשמור על אחידות עם הכרטיסים והמסננים.

## 5. UI ידידותי לנגישות

- **כפתורים גדולים:** יעד מגע ≥44×44px, מיקרופון בולט עם מצב ברור (מקשיב / מעבד / סיום).
- **טקסט ברור:** ניגודיות AA לפחות, גופן גדול, ללא הסתמכות על צבע בלבד למצב.
- **תמיכת מקלדת מלאה:** הפעלת מיקרופון, שליחה ועריכה נגישים ב-Tab/Enter/Space; אין מלכודות פוקוס; `Esc` עוצר הקלטה.
- **תוויות קורא-מסך:** `aria-label` עברי לכל פקד; אזור התמלול והתשובות כ-`aria-live="polite"` כך שקורא-מסך מכריז על שינויים; מצב המיקרופון מוכרז.
- **חלופות שקילות:** כל פעולה קולית זמינה גם בהקלדה; כל תשובת צ'אט קריאה כטקסט. אין פיצ'ר שזמין רק בקול.
- מתבסס על דפוסי הנגישות הקיימים ב-`accessibility-onboarding.tsx` ו-`accessibility-confidence.tsx`.

## 6. חיבור לפרופיל הנגישות

- מקור האמת נשאר `AccessibilityOnboardingProfile` ב-`src/lib/accessibility-profile.ts` (localStorage `trippilot:a11y-profile`).
- צרכים שזוהו בדיבור/צ'אט מתמפים אל אותו פרופיל: "כיסא גלגלים" → `mobilityDevice`, "ללא מדרגות" → `avoidStairs`, "הפסקות מנוחה" → `frequentRestBreaks` וכו', ואז עוברים דרך הממפים הקיימים (`deriveSearchPrefill`, `deriveHeroAccessibility`, `deriveMobilityLevel`, `deriveScoreProfileTypes`) — בלי לשכפל לוגיקה.
- **כיוון דו-סטרי:** אם כבר קיים פרופיל שמור, הצ'אט קורא אותו ("אני יודע שאת/ה משתמש/ת בכיסא גלגלים…") ומציע לעדכן; עדכון בדיבור עובר דרך `saveAccessibilityProfile` עם `parseAccessibilityProfile` (שמסנן קלט לא תקין, לא זורק). שינוי פרופיל תמיד דורש אישור מפורש, לא נקבע בשקט.

## 7. חיבור ליצירת המסלול

- הקול והצ'אט מזינים את אותו `CustomItineraryInput` ש-`generate-itinerary.ts` כבר מקבל: `destination`, `days`, `budget`, `tripType`, `preferences.accessibility` (`AccessibilityPreferences`), `selectedFlight`.
- צרכי נגישות שזוהו → `AccessibilityPreferences`; הערות וקישורים → הקשר ל-prompt. סוגי טיול נגישים קיימים ("אופני יד / אופניים מותאמים", "טיול קרוואנים נגיש למשפחות") ניתנים לבחירה בקול.
- זרימה: **קול/צ'אט → אישור → `CustomItineraryInput` → מסלול קיים.** אין מסלול חדש לתחזק. המסלול שנוצר ממשיך לכבד את כלל היושרה (נגישות לא מאומתת מסומנת).

## 8. כללי פרטיות ובטיחות

- **קול:** מתבצע בדפדפן דרך `Web Speech API`; מובהר למשתמש שחלק מהדפדפנים שולחים אודיו לשירות זיהוי של ספק הדפדפן. אין שמירת אודיו בשרת ב-MVP; שומרים תמלול טקסט בלבד, מקומית. כפתור "מחק תמלול".
- **הסכמה:** המיקרופון נדרש בלחיצה מפורשת בכל פעם; אין האזנה תמידית; אינדיקטור "מקשיב" גלוי בזמן הקלטה.
- **קישורים חיצוניים:** מטופלים כלא-מהימנים כברירת מחדל — אין fetch אוטומטי, אין הרצת תוכן; מוצג ה-URL המלא לפני שמירה. שליחה לספק AI חיצוני = פרסום החוצה: דורשת הסכמה והבהרה שהנתון עלול להישמר/להיות מאונדקס.
- **מינימום נתונים:** שולחים לספק AI רק את מה שצריך לתכנון; ברירת מחדל ללא PII רגיש. צרכי נגישות הם נתון רגיש — נשמרים מקומית, לא משותפים ללא הסכמה.
- **יושרה:** הצ'אט לא ממציא נגישות, מחירים או אישורי הזמנה; חוסר ודאות נאמר במפורש. אין ייעוץ רפואי.
- **בטיחות AI:** prompt-injection מקישורים/הערות מנוטרל — תוכן משתמש מטופל כנתונים, לא כהוראות; הצ'אט לא מבצע פעולות חיצוניות (הזמנה/תשלום) מתוך טקסט שהודבק.

## 9. היקף MVP

- כפתור מיקרופון + `Web Speech API` עברית, עם נפילה להקלדה.
- `parseTravelUtterance` דטרמיניסטי (מילון עברית) → `CustomItineraryInput` + `AccessibilityPreferences`, עם בדיקות יחידה.
- מסך אישור "הבנתי כך…" לפני יצירת מסלול.
- צ'אט נגישות בסיסי מעל ה-stub הקיים: מצב מקומי (FAQ + הקשר מהמסך) ללא ספק; חיבור ספק AI אופציונלי ולא חוסם.
- צירוף קישורים והערות כהקשר משתמש (ללא fetch); חוזה `attachments` מוגדר, תמונות **מוגדרות בלבד**.
- חיבור מלא לפרופיל (§6) וליצירת המסלול (§7); UI נגיש מלא (§5); כל הטקסטים עברית-first.
- פלט קולי בסיסי (§11): הקראת מסלול והקראת אזהרות נגישות בעברית, עם נפילה לטקסט.
- מצב נגישות מוגדל (§12): מתג יחיד לכפתורים גדולים + ניגודיות גבוהה + תאימות קורא-מסך.
- פקודות קוליות (§13): "בנה לי טיול", "היום הבא", "היום הקודם", "קרא הערות נגישות", "הצג מלונות נגישים" — דטרמיניסטי, עם חלופת כפתור לכל פקודה.
- **לא נכלל ב-MVP:** העלאת/ניתוח תמונות, שליפת מטא-דאטה מקישורים, חילוץ ישויות מבוסס-LLM כברירת מחדל, Wheelmap/OSM, קול במובייל native, ניווט קולי מלא "ללא עיניים".

## 10. היקף עתידי

- חילוץ ישויות מובנה מבוסס-LLM (Claude/`openai-agent`) עם נפילה למקומי.
- העלאת תמונות + תיאור נגישות (אנושי, ובהמשך אוטומטי) — לעולם לא כ"מאומת" בלי מקור.
- שליפת מטא-דאטה מקישורי מלון/אטרקציה (כותרת, שדות נגישות) מאחורי הסכמה ועם תיוג מקור/ודאות, בהמשך ממוזגת עם Google Places / Wheelmap (משימה 6).
- קול בערבית ואנגלית; TTS להקראת תשובות הצ'אט ובחירת קולות עבריים איכותיים (§11).
- הרחבת סט הפקודות הקוליות (§13) ופקודות מודעות-מסלול ("הוסף יום", "קצב רגוע יותר").
- ניווט קולי מלא "ללא עיניים" למטיילים עיוורים וכבדי-ראייה (§14), עם בדיקות שמישות מול קוראי-מסך ויעד WCAG 2.2 AA.
- צ'אט מודע-מסלול שמבצע עריכות ("הוסף יום", "החלף את היום השני בקצב רגוע יותר") — דרך אישור משתמש.
- סנכרון פרופיל מאומת לשרת (Prisma `UserPreference`) כשהאימות (auth) יחובר.

## 11. פלט קולי (Text-to-Speech)

- **מה המשתמש מקבל:** המערכת מקריאה בקול — הקראת המסלול היומי, והקראת אזהרות נגישות — בעברית כברירת מחדל.
- **גישה טכנית (MVP):** `Web Speech API` (`SpeechSynthesis`) בדפדפן עם `lang="he-IL"`, מאחורי שכבת `speak(text, { lang })` אחת. אין תלות בספק חיצוני ל-MVP; אם אין קול עברי זמין בדפדפן, נופלים אלגנטית להצגת טקסט בלבד ללא שגיאה.
- **הקראת המסלול:** כפתור "הקריאו לי את המסלול" שעובר על ימי ה-`ItineraryPlace` לפי סדר (בוקר/צהריים/ערב) וקורא שם, זמן מומלץ ותיאור קצר. תומך בעצירה/השהיה/המשך, ומסונכרן עם פקודות הקול "היום הבא"/"היום הקודם" (§13).
- **הקראת אזהרות נגישות:** כל סימון **"נגישות לא מאומתת — מומלץ לבדוק מול המקום"** ושדות `AccessibilityFact` בעלי ודאות נמוכה ניתנים להקראה מפורשת ("קרא הערות נגישות", §13). ההקראה אומרת במפורש את רמת הוודאות והמקור — לא מקריאה טענה כאילו אומתה. זהו כלל היושרה, באכיפה קולית.
- **עברית-first:** מפתחות `voice.tts.*` ב-`messages/he.json` תחילה; בחירת הקול מעדיפה `he-IL`. הקראה זמינה כתוספת ולא כתחליף לטקסט הנראה — כל מה שמוקרא גם מוצג על המסך.
- **נגישות הפקד:** כפתורי ההקראה גדולים, עם `aria-label` עברי, ומצב ברור (מקריא / מושהה / סיום) שמוכרז ל-`aria-live`.

## 12. מצב נגישות מוגדל (Large Accessibility Mode)

- **מה המשתמש עושה:** מפעיל מתג יחיד ("מצב נגישות מוגדל") שמחיל בבת אחת: כפתורים גדולים, ניגודיות גבוהה, ותאימות קורא-מסך משופרת. ההעדפה נשמרת מקומית (למשל `trippilot:large-a11y-mode`) ומיושמת בטעינה.
- **כפתורים גדולים:** הגדלת יעדי מגע (≥48×48px), טיפוגרפיה גדולה יותר ומרווחים מוגדלים — דרך class/data-attribute ברמת השורש, בלי לשכפל קומפוננטות.
- **מצב ניגודיות גבוהה (High Contrast):** ערכת צבעים בניגודיות מוגברת (מעבר ל-AA, יעד AAA לטקסט), ללא הסתמכות על צבע בלבד למצב; מכבד `prefers-contrast` ו-`prefers-color-scheme` כברירת מחדל ומאפשר דריסה ידנית. מתבסס על `theme-toggle.tsx` הקיים.
- **תאימות קורא-מסך:** מבנה כותרות סמנטי, `aria-label` עברי לכל פקד, אזורי `aria-live="polite"` לתמלול/תשובות/הקראה, ניהול פוקוס בלי מלכודות, ו-skip-links. נבדק מול קורא-מסך (NVDA/VoiceOver) כקריטריון קבלה.
- **חלופות שקילות:** המצב המוגדל לא מסתיר אף פיצ'ר — הוא רק מגדיל/מבהיר. מתיישב עם דפוסי §5 ועם `accessibility-onboarding.tsx` הקיים.

## 13. פקודות קוליות

- **מה המשתמש עושה:** במצב הקשבה אומר פקודה קצרה, והמערכת מבצעת פעולה במקום (לא רק מתמללת). זיהוי הפקודה דטרמיניסטי (מילון עברית עם מילים נרדפות), עם אישור קולי/חזותי קצר לפני פעולות שמשנות מצב.
- **סט הפקודות ל-MVP:**
  - **"בנה לי טיול"** / "תכנן טיול" → מפעיל יצירת מסלול מתוך הקלט שנאסף (§1, §7), אחרי מסך אישור "הבנתי כך…".
  - **"היום הבא"** → גלילה/מעבר ליום הבא במסלול ובהקראה.
  - **"היום הקודם"** → מעבר ליום הקודם.
  - **"קרא הערות נגישות"** → מפעיל הקראת אזהרות/הערות הנגישות של היום הנוכחי (§11), כולל רמת ודאות ומקור.
  - **"הצג מלונות נגישים"** → מפנה/מסנן לתצוגת מלונות עם מסנני הנגישות מהפרופיל (`deriveSearchPrefill`), בכבוד לכלל "נגישות לא מאומתת".
- **גישה טכנית:** שכבת `matchVoiceCommand(text)` (pure, בת-בדיקה) שממפה תמלול → enum פקודה, ואז dispatch אל הפעולות הקיימות. פקודה לא מזוהה → המערכת אומרת "לא הבנתי, אפשר לנסות שוב" ומציגה רשימת פקודות. כל פקודה זמינה גם ככפתור/קישור (חלופה שקולה, §5).
- **בטיחות:** פקודות לא מבצעות פעולות חיצוניות (הזמנה/תשלום) ולא משנות את פרופיל הנגישות בלי אישור מפורש (§8).

## 14. תמיכה עתידית במטיילים עיוורים וכבדי-ראייה

- **חזון:** זרימת תכנון מלאה הניתנת להפעלה ללא ראייה כלל — קלט קולי (§1) + פלט קולי (§11) + פקודות (§13) + מצב מוגדל/ניגודיות (§12) יוצרים יחד מסלול שלם "ללא עיניים".
- **כיווני עבודה עתידיים:**
  - מעבר קולי מלא על המסלול והתוצאות עם ניווט קולי (קדימה/אחורה/חזור/פרטים), בלי תלות במגע מדויק.
  - הקראה אוטומטית של אזהרות נגישות ושינויים חשובים (`aria-live` + TTS) בלי שהמשתמש יצטרך לאתר אותם ויזואלית.
  - בדיקות שמישות אמיתיות עם משתמשי NVDA/VoiceOver וקהל עיוור/כבד-ראייה כקריטריון קבלה.
  - תיאורי תמונה (alt עשיר) לתמונות נגישות שיועלו בעתיד (§3, §10) — אנושי תחילה, אוטומטי בהמשך, לעולם לא כ"מאומת" ללא מקור.
  - תאימות מלאה ל-WCAG 2.2 AA כיעד מוצהר, עם נתיב ל-AAA במצב המוגדל.

---

### חיבור למסמכים קיימים
- פרופיל נגישות → משימה 1 (Next Sprint, ממומש ב-`accessibility-profile.ts`).
- כלל יושרה / מודל ודאות → משימה 2.
- שליפת נגישות מקישורים/מקומות → תלוי במשימות 5–6 (Google Places, Wheelmap — **לא מתחיל כאן**).

_מסמך זה קובע כיוון בלבד. לא שונה קוד אפליקציה ולא בוצע commit._
